## Comments

- Do not add comments to non-public members. The code should speak for itself.
- XML doc comments (`///`) are only permitted on `public` members of `public` types.
- Inline comments (`//`) are not permitted. If the intent of code is not clear from reading it, refactor until it is.
- Comments on non-public code will drift from implementation and become lies. Prefer clear naming over explanation.
